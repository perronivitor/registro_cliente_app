<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/nav_home">

    <fragment
        android:id="@+id/nav_home"
        android:name="com.perroni.registroclienteapp.ui.home.HomeFragment"
        android:label="Menu Principal"
        tools:layout="@layout/fragment_home" >
        <action
            android:id="@+id/action_nav_home_to_nav_add_edit"
            app:destination="@id/nav_add_edit" />
    </fragment>

    <fragment
        android:id="@+id/nav_add_edit"
        android:name="com.perroni.registroclienteapp.ui.gallery.AddFragment"
        android:label="Adicionar"
        tools:layout="@layout/fragment_add_edit" />


</navigation>